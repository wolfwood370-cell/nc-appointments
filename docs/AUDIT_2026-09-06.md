# Audit progetto nc-calendar — 6 settembre 2026

Verifiche eseguite: compilazione produzione, controllo tipi, lint, scanner sicurezza
(Supabase + dipendenze + Wiz), linter database, integrità dati, cron, errori runtime/Sentry.

## Stato generale: buono

- Build di produzione: **OK** (18s, nessun errore).
- Controllo tipi TypeScript: **0 errori**.
- Dipendenze: **nessuna vulnerabilità** alta o critica.
- Errori runtime registrati: **nessuno**.
- Cron: entrambi attivi e con esecuzioni riuscite
  (`auto_complete_past_bookings` ogni 15 min, `auto_renew_recurring_blocks_daily` alle 04:00).
- Integrità dati: nessun credito in eccesso, nessun blocco sovrapposto o con buchi,
  nessun evento Google duplicato, nessun utente senza ruolo, nessuna sessione con fine
  precedente all'inizio.

## 🟡 Da sistemare (medio)

1. **Formattazione codice non applicata** — 1.706 errori di stile (Prettier) su tutto il
   progetto, comprese le funzioni backend. Non rompe nulla, ma il comando `lint` fallisce
   sempre e maschera problemi reali. Risolvibile automaticamente.
2. **58 variabili/import inutilizzati** (fra cui `useState`, `useQuery`, `SessionType` in
   `trainer.index.tsx`, `forecasts` in trainer.clients.$id) — residui di refactoring.
3. **8 hook React con dipendenze incomplete** — potenziale causa di dati non aggiornati
   dopo alcune azioni.
4. **Titoli pagina mancanti** — nessuna delle 19 pagine ha titolo/descrizione propri:
   in cronologia browser, condivisioni e risultati di ricerca appaiono tutte uguali.

## 🟢 Minori

- **Branding "Stride"** ancora presente nell'intestazione della pagina Amministrazione
  (`src/routes/admin.tsx:151`) — incoerente con "NC Calendar".
- **2 componenti mai usati**: `filter-chip.tsx`, `quick-stat.tsx` (codice morto).
- **3 sessioni senza cliente e non marcate come personali** — eventi importati da Google
  rimasti in uno stato ibrido; innocui ma sporcano le statistiche.
- **95 sessioni senza tipologia evento**: sono tutte importate da Google (impegni personali,
  compleanni, promemoria). Comportamento atteso → colore grigio di fallback.
- **12 impegni passati ancora "programmati"**: sono tutti impegni personali, che per scelta
  il completamento automatico non tocca. Atteso.
- **Sentry Replay** viene incluso anche nel bundle server (~256 KB) senza servire lì.

## ℹ️ Verificato e corretto per design

- `action_rate_limits` e `send_email_rate_limit` hanno RLS attiva senza policy: accessibili
  solo dal backend privilegiato. Corretto.
- Le segnalazioni del linter database sulle funzioni `SECURITY DEFINER` sono già state
  esaminate e ignorate in precedenza (servono per bypassare RLS in modo controllato).
- I punti aperti dell'audit del 6 giugno risultano chiusi (IDOR cross-coach, sync Google
  nelle modifiche coach, codice morto, recharts rimosso), tranne il branding "Stride".

## Ordine di intervento consigliato

1. Formattazione automatica + rimozione import inutilizzati (rischio nullo).
2. Titoli e descrizioni per pagina.
3. Branding "Stride" → "NC Calendar" e rimozione dei 2 componenti morti.
4. Revisione dei 8 hook con dipendenze incomplete (uno alla volta, con verifica).
5. Pulizia delle 3 sessioni ibride importate.
